import { render } from 'preact';
import { PerformanceModal } from './public/components/PerformanceModal.jsx';

// Since this is a Preact component that uses hooks, we can't easily run it outside a browser environment
// But we can check if there are syntax errors or obvious throws.
