# Allow only build outputs. `npm run build` (esbuild, see build.mjs) runs BEFORE
# `docker build`, on the CI runner or the developer's machine — the image never
# compiles anything, and never sees local DBs, env files, sources or tests.
**
!package.json
!package-lock.json
!dist/
!dist/**
!migrations/
!migrations/*.sql
