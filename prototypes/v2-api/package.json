{
    "name": "ensemble-v2-api",
    "private": true,
    "version": "0.0.0",
    "description": "Standalone Node service for Ensemble v2 accounts: schema, migrations and (in later stage-2 stories) WebAuthn ceremonies and sessions. Not wired into prototypes/v2 or public/ — see prototypes/v2-api/README.md.",
    "type": "module",
    "engines": {
        "node": ">=26"
    },
    "scripts": {
        "typecheck": "tsc --noEmit",
        "build": "tsc --project tsconfig.build.json",
        "test": "npm run typecheck && npm run build && vitest run --config vitest.api.config.ts",
        "start": "node dist/server.js",
        "dev": "tsx src/server.ts"
    },
    "devDependencies": {
        "@types/node": "^26.0.1",
        "tsx": "4.23.13",
        "typescript": "^7.0.2",
        "vitest": "^4.1.2"
    },
    "dependencies": {
        "@hono/node-server": "2.1.1",
        "@simplewebauthn/server": "14.0.1",
        "hono": "4.13.7"
    }
}
