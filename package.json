{
    "name": "ensemble",
    "description": "Generative musical accompaniment and chord visualization PWA",
    "version": "1.2.2",
    "license": "AGPL-3.0-only",
    "type": "module",
    "scripts": {
        "test": "npm run check-mutations && npm run lint && vitest run",
        "lint": "biome check .",
        "format": "biome check --write .",
        "check-mutations": "grep -rnE \"(playback|chords|bass|soloist|harmony|groove|midi|vizState)\\.[a-zA-Z0-9]+ = \" public/components/ public/*.js | grep -v \"state/\" | grep -v \"reducer\" | grep -v \"@direct-mutation\" | grep -v \"@worker-mutation\" || true",
        "deploy:test": "./scripts/deploy-test.sh",
        "deploy:prod": "./scripts/deploy-prod.sh"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.4.6",
        "esbuild": "^0.27.3",
        "happy-dom": "^20.8.3",
        "knip": "^5.85.0",
        "vitest": "^4.0.18"
    },
    "dependencies": {
        "preact": "^10.28.4"
    }
}
